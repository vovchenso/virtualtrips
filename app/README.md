## Virtualtrips FRONTEND APP

### Install

Run `yarn` to install all the dependencies

### Run the app

Run `yarn start` to start the app. It will run it on port `7080` (by default).

### TODOs

- Add tests
